<UserControl x:Class="QuickDoc.View.SpecificItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuickDoc.View"
             xmlns:vm="clr-namespace:QuickDoc.ViewModel"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="650" d:DesignWidth="500">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:ItemViewModel}">
            <StackPanel>
                <Border BorderBrush="#014e99" BorderThickness="2" Margin="20">
                    <StackPanel Margin="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Item: " FontSize="16"/>
                            <TextBlock Text="{Binding ItemNumber}" FontSize="16" />
                        </StackPanel>
                        <TextBlock Text="{Binding Description}" FontSize="14" Margin="5,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Quantity: " Width="100"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Quantity}" FontSize="12" Margin="5,0,0,0"/>
                                <TextBlock Text="{Binding UnitOfMeasure}" FontSize="12" Margin="5,0,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="LineNumber: " Width="100"/>
                            <TextBlock Text="{Binding LineNumber}" FontSize="12" Margin="5,0,0,0"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Serial number: " Width="100"/>
                            <TextBlock Text="{Binding SerialNumber}" FontSize="12" Margin="5,0,0,0"/>
                        </StackPanel>

                    </StackPanel>
                </Border>

                <Border BorderBrush="#014e99" BorderThickness="2" Margin="20">
                    <StackPanel>
                        <TextBlock Text="Serial number:" Margin="20,10,20,0" FontSize="16"/>
                        <StackPanel Orientation="Horizontal" Margin="20,0,20,20" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBox MinWidth="250" VerticalContentAlignment="Center" Text="{Binding SerialNumber, UpdateSourceTrigger=LostFocus}"/>
                            <Button Padding="20" Command="{Binding DataContext.UPDATESERIAL, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content=" 💾 " Height="40" Width="40"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Blue bar at the top -->
        <Grid x:Name="PentairLogo" Grid.Row="0" Background="WhiteSmoke">
            <Grid.Effect>
                <DropShadowEffect/>
            </Grid.Effect>
            <Image Margin="0,20,20,20" Source="pack://siteoforigin:,,,/Images/Pentair_Schroff_Logo.png" HorizontalAlignment="Right" Height="35"/>
            <Button Content="↩"
                    Grid.Column="0"
                    HorizontalAlignment="Left"
                    Margin="20"
                    VerticalAlignment="Center"
                    Width="40"
                    Height="40"
                    Background="#FF538CC5"
                    Foreground="Black"
                    Command="{Binding GOBACK}"
                    CommandParameter="{Binding}">
                <Button.Resources>
                    <SolidColorBrush x:Key="Button.HoverBackground" Color="#FF9BC9F5" />
                    <SolidColorBrush x:Key="Button.PressedBackground" Color="#FF2F6FAE" />
                    <SolidColorBrush x:Key="Button.DefaultBackground" Color="#FF538CC5"/>
                </Button.Resources>
            </Button>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Content="Project:" VerticalAlignment="Top" FontSize="14" FontWeight="Bold" Width="120"/>
                <Label Content="{Binding currentProjectNumber}" VerticalAlignment="Top" FontSize="14" Width="120"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1">

            <ContentPresenter Content="{Binding CurrentNode}" />

            <Border BorderBrush="#014e99" BorderThickness="2" Margin="20">
                <StackPanel Margin="10">
                    <TextBlock Text="Documents:" FontSize="16"/>
                    <ListBox Grid.Row="2" ItemsSource="{Binding Documents}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Description}" FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
