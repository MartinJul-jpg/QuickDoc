<UserControl x:Class="QuickDoc.View.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuickDoc.View"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="650" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <!-- Blue bar at the top -->
        <Grid x:Name="PentairLogo" Grid.Row="0" Background="#014e99">
            <Image Margin="260,5,5,3" Source="pack://siteoforigin:,,,/Images/Pentair_Schroff_Logo.png"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="31*"/>
                <RowDefinition Height="6*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Criteria boxes -->
            <StackPanel Grid.Row="0" Grid.Column="0">
                <StackPanel Margin="20,15,20,15">
                    <Label Content="Project" HorizontalAlignment="Left"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding Criteria.ProjectCriteria}" VerticalAlignment="Top" FontSize="16"/>
                </StackPanel>

                <StackPanel Margin="20,15,20,15">
                    <Label Content="Unit" HorizontalAlignment="Left"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding Criteria.UnitCriteria}" VerticalAlignment="Top" FontSize="16"/>
                </StackPanel>

                <StackPanel Margin="20,15,20,15">
                    <Label Content="Section" HorizontalAlignment="Left"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding Criteria.SectionCriteria}" VerticalAlignment="Top" FontSize="16"/>
                </StackPanel>

                <StackPanel Margin="20,15,20,15">
                    <Label Content="Tag" HorizontalAlignment="Left"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding Criteria.TagCriteria}" VerticalAlignment="Top" FontSize="16"/>
                </StackPanel>

                <StackPanel Margin="20,15,20,15">
                    <Label Content="Item" HorizontalAlignment="Left"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding Criteria.ItemCriteria}" VerticalAlignment="Top" FontSize="16"/>
                </StackPanel>
            </StackPanel>

            <Button Name="EnterButton" Grid.Column="1" Content="Enter" Grid.Row="1" Margin="20,20,20,20" Background="#5cbb4e" FontWeight="Bold" FontSize="14" Command="{Binding GETBYCRITERIA}" Click="EnterButton_Click"/>

        </Grid>
    </Grid>
</UserControl>
